#build a module - area of shapes

#area of a rectangle
def calc_area_rect(length, width):
    area = length * width
    print(area)

#area of a triangle
def calc_area_tri(base, height):
    area = 0.5 * (base * height)
    print(area)